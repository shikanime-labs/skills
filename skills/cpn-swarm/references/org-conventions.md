# cpn-swarm — conventions d'org (cloud-pi-native)

À lire quand vous distribuez une tâche org sur un cluster d'agents.

- Dépôt par défaut pour la réconciliation : `cloud-pi-native/console`
  (`gh issue view <N> --repo cloud-pi-native/console`).
- Enregistrer la liste des unités dans l'issue liée avant l'envoi.
