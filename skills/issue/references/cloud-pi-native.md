# Org reference: cloud-pi-native

Consumed by `skills/issue/SKILL.md` to select cloud-pi-native behaviour.

## Target repos

- `cloud-pi-native/console` (always this repo).

## Behaviour

- **language**: fr — write issue bodies and comments in French.
- **commit convention**: conventional — conventional commit titles; Release Please drives versioning.
- **co-author trailer**: none.
- **title prefix**: bug → `🐛 [BUG] - `, feature → `💡 [REQUEST] - `.
- **label defaults**: `bug` for defects, `enhancement` for features.
