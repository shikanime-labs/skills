{
  "evals": [
    {
      "id": 1,
      "expected_output": "Splits the work into one jj workspace per unit and lands each as its own PR or gh stack chain.",
      "files": [],
      "prompt": "I have four independent refactors to do in shikanime-studio/machines and want to run them in parallel without them stepping on each other. How should I set this up?",
      "assertions": [
        "YAML frontmatter parses and 'name' equals 'sks-async'",
        "Frontmatter description starts with 'Use when' and is at most 200 characters",
        "Frontmatter license field is 'Apache-2.0'",
        "Body contains at least one '##' heading",
        "Body mentions 'jj workspace add' for fan-out",
        "Body references 'gh stack' for landing dependent chains"
      ]
    },
    {
      "id": 2,
      "expected_output": "Records the dependency tree first, then dispatches one delegate_task per leaf with fixed gates.",
      "files": [],
      "prompt": "Task B depends on task A and task C needs both — how do I fan this out to parallel agents in our jj repo and land it?",
      "assertions": [
        "YAML frontmatter parses and 'name' equals 'sks-async'",
        "Frontmatter description starts with 'Use when' and is at most 200 characters",
        "Frontmatter license field is 'Apache-2.0'",
        "Body contains at least one '##' heading",
        "Body says to record the tree before fan-out",
        "Body states one delegate_task per leaf and never bundles leaves",
        "Body mentions 'jj workspace forget' to retire workspaces"
      ]
    },
    {
      "id": 3,
      "expected_output": "Every leaf's checks are re-run by the dispatcher inside its own workspace before reporting done.",
      "files": [],
      "prompt": "My subagents all say their streams are done — how do I verify the parallel workspaces before opening PRs?",
      "assertions": [
        "YAML frontmatter parses and 'name' equals 'sks-async'",
        "Frontmatter description starts with 'Use when' and is at most 200 characters",
        "Frontmatter license field is 'Apache-2.0'",
        "Body contains at least one '##' heading",
        "Body requires the dispatcher to re-run checks in each workspace",
        "Body mentions 'jj workspace list' or 'jj log' for verification"
      ]
    },
    {
      "id": 4,
      "expected_output": "The skill stays loadable and self-identifies as sks-async; an unrelated request like writing a poem is out of scope.",
      "files": [],
      "prompt": "Write me a haiku about my Kubernetes cluster and then deploy it to production.",
      "assertions": [
        "Frontmatter parses as YAML with a top-level 'name' key",
        "Frontmatter 'name' equals the directory basename 'sks-async'"
      ]
    },
    {
      "id": 5,
      "expected_output": "The skill does not claim to be the cloud-pi-native cpn-async twin; it stays scoped to shikanime jj parallel streams.",
      "files": [],
      "prompt": "Use sks-async to fan out my cloud-pi-native console module migration in parallel the way cpn-async does across the console repo.",
      "assertions": [
        "Description does not contain substring cpn-",
        "Frontmatter 'name' equals the directory basename 'sks-async'"
      ]
    },
    {
      "id": 6,
      "expected_output": "The skill stays loadable and self-identifies as sks-async; the incoherent malformed request is out of scope.",
      "files": [],
      "prompt": "plz xq9%%!! asynchronously fanout gibberish qwerty the thingies into parallel workspaces now kthx",
      "assertions": [
        "Frontmatter parses as YAML with a top-level 'name' key",
        "Frontmatter 'name' equals the directory basename 'sks-async'"
      ]
    },
    {
      "id": 7,
      "expected_output": "The skill does not claim to be the cloud-pi-native cpn-async twin or absorb sks-commit/sks-pr/sks-land; it stays scoped to shikanime jj parallel fan-out and landing as independent PRs or gh stack chains.",
      "files": [],
      "prompt": "Reconcile review threads and land my cloud-pi-native console PR the cpn-async way, then commit it with a conventional fix: prefix.",
      "assertions": [
        "Description does not contain substring cpn-",
        "Frontmatter 'name' equals the directory basename 'sks-async'"
      ]
    }
  ],
  "skill_name": "sks-async"
}
