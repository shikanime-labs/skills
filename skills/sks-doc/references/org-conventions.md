# Org conventions

Repo scoping for docs work: target repos live under `shikanime-labs/*` /
`shikanime-studio/*`. Validate `R` against both before editing; outside them,
confirm the target repo with the user first.
