# Org conventions

Canonical checkouts live at `~/Source/Repos/github.com/<org>/<repo>` — run GC
from that repo root. Skill-created workspaces are siblings of it
(`../<repo>.<unit>`, `../<repo>.fix`), never subdirectories.
