# Org conventions

- Scope: issues are opened in `shikanime-labs/*` / `shikanime-studio/*` only.
  Outside both orgs, confirm the target repo with the user first.
- English only. The cpn family's issue templates are French — never carry
  them (or their headings) into these repos.
